# Supabase — safe to expose to the client
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# TMDB — server-side only, NEVER exposed to the client.
# Either a v3 API key or a v4 read access token (JWT) works.
TMDB_API_KEY=your-tmdb-key
